READ ME 

Jacob Farner and Leopold Ringmayr


To run the naive Bayes model, run the bayesModel.py file.
To run the logistic regression model, run the regression.py file.

For both models the data is stored locally as ‘spam 2.py’ in the PyCharm project. And both files require the the NLTK and Pandas libraries. 

